# ./src/Routes/tags.yaml

show:
    path: /{_locale}/{id}/{slug}
    controller: App\Controller\TagsController::showAction
    requirements:
        id:   '[1-9][0-9]*'
        slug: '[a-z][a-z0-9\-]*'
